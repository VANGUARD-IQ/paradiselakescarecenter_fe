overwrite: true
schema: http://localhost:4000/graphql
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
    config:
      scalars:
        Date: string
        DateTime: string
        Upload: File